{
  "name": "youtube-music",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "electron": "^2.0.3",
    "rxjs": "^6.2.1"
  }
}
